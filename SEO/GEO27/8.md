# SEO + GEO AUDIT — hmimi.design
**Date:** 27/08/2026

---

## CRITICAL: Deployment Mismatch

**Live site serves OLD Vite SPA, not Next.js.** Fetched pages show `<div id="root"></div>` + `/assets/index-*.js` — identical HTML across all routes. All `generateMetadata()` and `StructuredData` component code in `app/` **is not live**.

**Impact: HIGH** — Every page-specific fix below is dormant until Next.js is deployed.

---

## TECHNICAL SEO

| # | Issue | Severity | Page/Line |
|---|-------|----------|-----------|
| 1 | **All pages serve identical HTML** — same title, description, canonical, OG tags on every URL | CRITICAL | Live site |
| 2 | **Canonical URL hardcoded to `/`** on all pages | HIGH | `app/layout.tsx:32` |
| 3 | **Duplicate font loading** — `@import url()` in CSS + `next/font/google` in layout | MEDIUM | `app/globals.css:1-2`, `app/layout.tsx:7-17` |
| 4 | **Favicon mismatch** — live: `/vite.svg`, Next.js: `/favicon.svg` | LOW | Live vs `app/layout.tsx:43` |
| 5 | **Blog slug fragility** — `title.toLowerCase().replace(...)` can produce collisions | MEDIUM | `app/sitemap.ts:34` |
| 6 | **No `og:image:width` / `og:image:height`** on any page | MEDIUM | All metadata exports |
| 7 | **Missing `geo.region`, `geo.placename`** meta tags | LOW | `app/layout.tsx` |

---

## ON-PAGE SEO

| # | Issue | Severity | Location |
|---|-------|----------|----------|
| 8 | **Homepage title too long** (62 chars, truncated in SERP) | MEDIUM | `app/page.tsx:6` |
| 9 | **No `<meta name="keywords">`** anywhere | LOW | All pages |
| 10 | **About section missing from homepage** — footer links to `#about` but `HomeClient` doesn't render `About` component | HIGH | `components/sections/HomeClient.tsx` |
| 11 | **Project pages: no `StructuredData` rendered** | HIGH | `app/projects/[id]/page.tsx:31` |
| 12 | **Resources page: no `StructuredData` rendered** | MEDIUM | `app/resources/page.tsx:14` |
| 13 | **Blog post pages: no `og:image`** (no cover image passed) | MEDIUM | `app/blog/[slug]/page.tsx:24-31` |
| 14 | **Blog posts are thin content** — only title + "Read on Medium" link, no on-site article body | HIGH | `app/blog/[slug]/BlogPostClient.tsx` |

---

## STRUCTURED DATA

| # | Issue | Severity | Location |
|---|-------|----------|----------|
| 15 | **Person `sameAs` missing 2 profiles** — dribbble + behance not in live Person schema | MEDIUM | `components/seo/StructuredData.tsx:20-25` vs live HTML |
| 16 | **Person `sameAs` URLs mismatched** — live has `linkedin.com/in/hmimi-chioukh`, code has `linkedin.com/in/hmimi-chioukh` (correct) but GitHub URL differs | LOW | `StructuredData.tsx:23` |
| 17 | **Article schema missing `image`** | MEDIUM | `StructuredData.tsx:53-75` |
| 18 | **No `BreadcrumbList` schema** on any page | MEDIUM | All pages |
| 19 | **No `Review` / `AggregateRating` schema** for testimonials | LOW | All pages |
| 20 | **No `FAQPage` schema** | LOW | All pages |
| 21 | **No `WebSite` SearchAction** schema | LOW | `StructuredData.tsx:41-51` |
| 22 | **`publisher` uses Person type** instead of Organization | LOW | `StructuredData.tsx:66-68` |

---

## GEO (AI Search Optimization)

| # | Issue | Impact | Recommendation |
|---|-------|--------|----------------|
| 23 | **No FAQPage schema** — AI engines cite FAQ schema +40% | HIGH | Add FAQ sections to project pages + homepage |
| 24 | **No statistics/numbers in structured content** | HIGH | Add metrics to case studies (users, revenue, NPS) |
| 25 | **No authoritative citations** in content | MEDIUM | Reference design principles, industry reports |
| 26 | **Blog content external (Medium)** — AI engines can't cite your domain | HIGH | Import full blog content to on-site pages |
| 27 | **No `llms.txt` or `llms-full.txt`** | MEDIUM | Create AI-readable site summary for LLM crawlers |
| 28 | **robots.txt doesn't explicitly allow AI bots** | MEDIUM | Add explicit `Allow` for `GPTBot`, `ClaudeBot`, `PerplexityBot` |
| 29 | **No PDF resume on homepage** for citation | LOW | CV is in `/public` but not linked from homepage |
| 30 | **Missing `knowsAbout` depth** — only 7 skills listed | LOW | Expand to 12+ with specific AI/UX terms |

---

## SITEMAP & CRAWLABILITY

| # | Issue | Severity |
|---|-------|----------|
| 31 | **Blog slugs generated from title** — if title changes, slug breaks, no redirect | MEDIUM |
| 32 | **All `lastModified` dates are identical** (2026-08-21) | LOW |
| 33 | **No `changefreq` differentiation** — blog = monthly, projects = monthly, homepage = weekly | LOW |

---

## CONTENT & E-E-A-T

| # | Issue | Severity |
|---|-------|----------|
| 34 | **No author byline on blog posts** visible to crawlers | MEDIUM |
| 35 | **No date visible on blog posts** | MEDIUM |
| 36 | **Testimonials lack schema markup** — CEO names + company = authority signals unused | MEDIUM |
| 37 | **No case study metrics visible in structured data** — outcomes exist but not in schema | HIGH |

---

## PRIORITIZED ACTION PLAN

### Immediate (deploy blockers)
1. **Deploy Next.js to production** — all SEO improvements are dormant
2. **Fix `#about` link** — add `<section id="about">` or remove broken footer link
3. **Remove duplicate font `@import`** from `globals.css`

### High Impact
4. **Add `StructuredData` to project pages** — pass project data to `StructuredData` component
5. **Import blog content on-site** — full article body, not just Medium links
6. **Add `BreadcrumbList` schema** to all pages
7. **Create `llms.txt`** at root with site summary
8. **Add statistics to case study schemas** (users, revenue, timeline)
9. **Expand Person `sameAs`** — add dribbble, behance to live schema

### Medium Impact
10. Fix homepage title length (≤60 chars)
11. Add `og:image:width` / `og:image:height` to all pages
12. Add `image` to Article schema
13. Add explicit AI bot permissions to robots.txt
14. Add `geo.region` + `geo.placename` meta tags

### Quick Wins
15. Fix favicon reference (`/favicon.svg` not `/vite.svg`)
16. Add FAQ sections to project pages
17. Add `WebSite` SearchAction schema
18. Fix `publisher` type to Organization
